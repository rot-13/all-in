
%header
  .container.padded
    %h1= t('.title')
    = link_to new_event_path, class: 'btn with-icon' do
      %i.fa.fa-plus
      = t('.new_event')

.rows
  - @events.each do |event|
    = link_to event, class: 'row' do
      %span.primary= event.title
      %span.secondary= event_date(event)
